{
  "buildCommand": "cd web && npm install && npm run build",
  "outputDirectory": "web/dist",
  "rewrites": [
    { "source": "/report/(.*)", "destination": "/index.html" },
    { "source": "/((?!assets|synapse-logo|favicon|_vercel).*)", "destination": "/index.html" }
  ]
}
